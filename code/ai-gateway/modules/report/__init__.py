"""
Report orchestration: windowing (`compute_fusion_meta`), stub body (`build_stub_report`),
WS resolution (`resolve_report_body`, `build_report_ws_message` in `orchestrator.py`).

The asyncio driver remains in `report_loop.py` (partial ticks + final on disconnect).
"""
